<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

<div class="fixed bottom-0 bg-orange-400 h-16 w-full">
    <div class="h-full grid grid-cols-3 grid-row-1 text-center content-center">
        <nav id="nav-hamburger">
            <button id="hamburgerBtn" onclick="toggleMenu()">
                <i id="hamburgerMenu" class="fa-solid fa-xl fa-bars"></i>
                <div class="fixed grid bottom-16 bg-orange-300 w-1/3 left-0 text-xl">
                    <div class="menuItems border-1 border-orange-400"></div>
                    <a link href="/setting" class="menuItems mx-auto py-2">Settings</a>
                    <div class="menuItems border-1 border-orange-400"></div>
                    <a link href="/about_us" class="menuItems mx-auto py-2">About Us</a>
                    <div class="menuItems border-1 border-orange-400"></div>
                    <a link href="/logout" class="menuItems mx-auto py-2">Logout</a>
                </div>
            </button>
        </nav>
        <a href="/home" class="flex items-center justify-center">
            <i class="fas fa-home text-black mr-2"></i>
        </a>
        <a href="/profile" class="flex items-center justify-center">
            <i class="fas fa-user text-black mr-2"></i>
        </a>
    </div>
</div>

<script defer>
    const menuItems = document.querySelectorAll('.menuItems');
    menuItems.forEach((menuItem) => {
        menuItem.style.display = 'none';
    });
    const navHam = document.getElementById("nav-hamburger");
    navHam.style.margin = '6px';
    navHam.style.marginTop = '14px';

    const hamburgerMenu = document.getElementById("hamburgerMenu");
    hamburgerMenu.style.display = 'flex';
    hamburgerMenu.style.alignItems = 'baseline';

    const hamburgerBtn = document.getElementById("hamburgerBtn");
    hamburgerBtn.style.position = 'fixed';

    function toggleMenu() {
        if (hamburgerMenu.classList.contains("fa-bars")) {
            hamburgerMenu.classList.remove("fa-bars");
            hamburgerMenu.classList.add("fa-burger");
        } else {
            hamburgerMenu.classList.add("fa-bars");
            hamburgerMenu.classList.remove("fa-burger");
        }
        const navLinks = document.querySelectorAll('.menuItems');
        navLinks.forEach((navLink) => {
            if (navLink.style.display === 'flex') {
                navLink.style.display = 'none';
            } else {
                navLink.style.display = 'flex';
            }
        });
    }
</script>