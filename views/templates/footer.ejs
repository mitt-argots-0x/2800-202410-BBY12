<button id="home"  onclick='window.location.href="/home"'>Home</button>
<button id="profile"  onclick='window.location.href="/profile"'>Profile</button>
<% if(typeof(jsFile) != 'undefined') { %>
    <script src="<%= jsFile %>"></script>
    <% } %>
</body>
</html>