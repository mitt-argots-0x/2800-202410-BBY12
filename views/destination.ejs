<%- include("templates/header") %>

    <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold mb-4"><%= city %></h1>
        <div>

    
            <% if (imageUrl) { %>
                <img src="<%= imageUrl %>" alt="Image of <%= city %>" class="w-full h-48 object-cover rounded mt-4">
            <% } %>
        </div>
        <a href="/review" class="mt-4 inline-block text-blue-500 hover:underline">See reviews</a>
    </div>
    

    <%- include("templates/homenav") %>
<%- include("templates/footer") %>